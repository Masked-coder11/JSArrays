class Solution {
    arraySortedOrNot(arr) {
        // code here
        let n=arr.length
        for(let i=1; i<n; i++){
            if(arr[i]<arr[i-1]) return false;
        }
        return true
    }
}
