class Solution {
    rotate(arr) {
        // code here
        let n = arr.length
        let j=n-2, i=n-1
        while(j>=0){
            [arr[j], arr[i]] = [arr[i], arr[j]]
            j--;
            i--;
        }
        return;
    }
}
